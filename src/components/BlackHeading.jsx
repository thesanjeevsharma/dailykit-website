import React from "react";
import "./../style.css";
const BlackHeading = ({ text }) => {
  return (
    <div>
      <h2 className="black_heading">{text}</h2>
    </div>
  );
};

export default BlackHeading;
