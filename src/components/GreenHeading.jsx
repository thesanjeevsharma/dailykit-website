import React from "react";
import "./../style.css";
const GreenHeading = ({ text }) => {
  return (
    <div>
      <h2 className="green_heading">{text}</h2>
    </div>
  );
};

export default GreenHeading;
